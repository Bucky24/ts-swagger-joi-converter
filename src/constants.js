const standardTypes = ['string', 'number', 'date', 'object'];

const Types = {
    Model: 'MODEL',
    Enum: 'ENUM'
};

const OutputTypes = {
	Json: 'OUTPUT_JSON',
	File: 'OUTPUT_FILE'
};

module.exports = {
    standardTypes,
    Types,
	OutputTypes
};